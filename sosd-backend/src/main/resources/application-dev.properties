# src/main/resources/application-dev.properties
# ==========================================================
# Spring Dev Profile - ?? ???? ???? ??
# ==========================================================

# ----------------------
# DB ??
# ----------------------
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----------------------
# JPA / Hibernate
# ----------------------
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.defer-datasource-initialization=false

# dev ??? ?? ???
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# !! dev, prod ????? ?? DB? ?? ??? ???? seed ?? ??
spring.sql.init.mode=never

# ----------------------
# GitHub token (env?? ??)
# ----------------------
github.token=${GITHUB_TOKEN}

# ----------------------
# .env ?? ??
# dev ???? docker-compose? env? ?????,
# ?? ?? jar? ?? ?? .env.dev ?? ? ???? ??? ??
spring.config.import=optional:file:.env.dev[.properties]

# ----------------------
# Logging
# ----------------------
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
logging.level.org.springframework.jdbc.datasource.init=INFO
logging.level.com.zaxxer.hikari=INFO

# ?? ?? ?? ??
logging.file.name=./logs/sosd-backend-dev.log
logging.logback.rollingpolicy.max-file-size=200MB
logging.logback.rollingpolicy.max-history=14
