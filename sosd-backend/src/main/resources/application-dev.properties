# ==========================================================
# Spring Dev Profile - ?? ??? ??
# ==========================================================

# MySQL ?? ?? (.env.dev?? ? ??)
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT:4410}/${MYSQL_DATABASE:github_crawl}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:sosd123!!!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# SQL ??? ???? (?? DB ??)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Hibernate ??
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# GitHub Token (.env.dev?? ??)
github.token=${GITHUB_TOKEN}

# .env.dev ?? ??
spring.config.import=optional:file:./.env.dev[.properties]

# --------------------------------------------------
# Logging ??
# --------------------------------------------------

# Hibernate ? SQL ?? ?? (INFO? ??)
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO
logging.level.org.springframework.jdbc.datasource.init=INFO
logging.level.com.zaxxer.hikari=INFO

# ?? ?? ??
logging.file.name=./logs/sosd-backend-dev.log
logging.logback.rollingpolicy.max-file-size=200MB
logging.logback.rollingpolicy.max-history=14

# --------------------------------------------------
# ?? ??
# --------------------------------------------------

# DB ??? ?? ?? ? ??
spring.sql.init.continue-on-error=false
spring.sql.init.encoding=UTF-8

# JPA ?? ?? ??
spring.jpa.open-in-view=false